package com.codegym.task.task39.task3905;

/* 
Fill 'er up

*/

public class Solution {


    public static void main(String[] args) {

/*        PhotoPaint photoPaint = new PhotoPaint();

        Color[][] image = new Color[3][7];
        for (int y = 0; y < image.length; y++) {
            for (int x = 0; x < image[0].length; x++) {
                image[y][x] = Color.RED;
            }
        }

        printArray(image);

        System.out.println("---------------------------");
        boolean canChangeColor = photoPaint.paintFill(image, 0, 2, Color.BLUE);
        System.out.println(canChangeColor);

        printArray(image);*/


    }

/*    private static void printArray(Color[][] image) {
        for (int y = 0; y < image.length; y++) {
            for (int x = 0; x < image[0].length; x++) {
                System.out.print(image[y][x] + " y= " + y + " x= " + x + "   ");
            }
            System.out.println();
        }
    }*/
}
